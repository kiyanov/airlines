import { Injectable } from '@angular/core';import {airports, generateFlights} from "../models/flights.sample-data";import {Flight, SearchOptions} from "../models/models";import {Observable, of} from "rxjs";import {filter} from "rxjs/operators";@Injectable({  providedIn: 'root'})export class FlightService {  private flights: Flight[];  constructor() {      this.flights = generateFlights(100);  }  searchFlights(options: SearchOptions): Observable<Flight[]>{    return of(this.flights).pipe(filter((flight: Flight[]) => {      if (options.arrival) {        return options.arrival === flight[0].arrival.airport.iata;      }      return true;    }));  }  getAirports() {    return of(airports);  }}